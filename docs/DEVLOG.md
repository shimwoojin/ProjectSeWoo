# DEVLOG

---

## 2026-09-01 — Day 0~1 · 프로젝트 셋업 + 오버레이 셸 스파이크

### 한 일

1. **기획서 검토 결과를 Week 0 검증 계획으로 확정** → [WEEK0-GODOT-VALIDATION.md](WEEK0-GODOT-VALIDATION.md)
   세 기획(끼끼 아일랜드 / Punchy / Tiny Quest)이 껍데기를 공유하므로, 기획 확정보다 껍데기 검증이 먼저라는 결론.
2. **GDScript 전용 → C# (.NET) 전환**
3. **Day 1-2 오버레이 셸 스파이크 구현** → [DAY1-2-SPIKE.md](DAY1-2-SPIKE.md)
4. **프로젝트명 `PcIdle` → `ProjectSeWoo`**

### 결정 사항

| 결정 | 이유 |
|---|---|
| **C# 단일 언어** (Godot .NET 빌드) | 네이티브 입력 후킹을 P/Invoke로 바로 할 수 있다. GDScript면 GDExtension C++ 모듈을 따로 빌드해야 하는데 1달 프로젝트에서 비현실적 |
| **`net8.0` 타깃** | Godot 4.4+ 기본값. 이 PC에 .NET 8 런타임(8.0.17)이 있어 그대로 실행됨. SDK는 9.0.301이지만 net8.0 빌드/실행 모두 정상 |
| **`Godot.NET.Sdk/4.7.2`** | 설치된 엔진 버전과 일치시킴 |
| **씬을 코드로 구성** | 스파이크 한정. `.tscn`은 루트 노드 하나뿐이고 나머지는 `OverlayShell.BuildScene()`이 만든다. 에디터를 안 거쳐도 상태 전부가 한 파일에서 읽히고, diff가 깨끗하다. **게임 본편에서는 에디터에서 씬을 짜야 한다** |
| **`window/stretch/mode="disabled"`** | passthrough 폴리곤 좌표가 창 픽셀 기준이라 스트레치가 걸리면 클릭 영역이 어긋난다 |
| **HUD 문자열 전부 ASCII** | 아래 함정 3 참고 |

### 밟은 함정 (다음에 또 밟지 말 것)

**1. Godot 표준 빌드는 C#을 못 읽는다**
`Godot_v4.7.2-stable_win64.exe`(표준)와 `Godot_v4.7.2-stable_mono_win64`(.NET)는 별도 다운로드다.
표준은 단일 exe, mono는 **폴더째**라 exe만 빼내면 실행되지 않는다.

**2. `Godot.Environment` vs `System.Environment` 이름 충돌**
`using Godot;` 상태에서 `Environment.ProcessorCount`를 쓰면 CS0104 모호성 에러.
`System.Environment`로 정규화해야 한다. `Godot.Environment`는 3D 월드 환경 리소스라 이름이 겹친다.

**3. ~~Godot 기본 테마 폰트에 한글 글리프가 없다~~ — 오진이었다 (2026-09-16 정정)**
당시 Label 한글이 두부(□)로 나온 것을 보고 "기본 폰트에 글리프가 없다"고 적었고,
그래서 HUD를 전부 영어로 썼다. **그런데 A6 옵션 창은 폰트를 번들하지 않고도 한글이
정상으로 나온다** — 리포지토리에 `.ttf`/`.otf` 가 하나도 없고 `AddThemeFontOverride`
도 안 쓰는데 "설정 / 크기 / 투명도 / 위치 잠금 / 커서 장식" 이 전부 제대로 렌더된다.
→ 한글 UI 에 폰트 번들이 **필수는 아니다.** 원인은 따로 있었을 것이다(당시 씬/테마
구성으로 추정). 디자인상 원하는 서체를 쓰려면 번들하면 되고, 그때 라이선스를
확인하면 된다(Pretendard / 나눔 계열). **"안 나온다"를 전제로 계획을 잡지 말 것.**

**4. `per_pixel_transparency/allowed`는 런타임에 못 켠다**
`project.godot`에서 켜야 한다. 코드에서 켜려 하면 조용히 무시된다.

**5. 드래그 중에는 passthrough를 꺼야 한다**
클릭 영역(스프라이트 사각형) 밖으로 커서가 나가는 순간 마우스 이벤트가 끊겨 드래그가 풀린다.
`BeginDrag()`에서 빈 배열을 넘겨 창 전체가 마우스를 받게 하고, `EndDrag()`에서 복구한다.
추가로 창 밖으로 나가면 모션 이벤트 자체가 안 오므로, 위치 추적은 `_Process`에서 `DisplayServer.MouseGetPosition()` 폴링으로 한다.

### 구현 메모 — 플리커를 "실험"으로 만든 이유

[godot#80098](https://github.com/godotengine/godot/issues/80098): 투명창 + `window_set_mouse_passthrough` 조합에서 흰색 깜빡임 리포트.
이게 Day 1-2 최대 불확실성인데, 문서만 봐서는 "엔진이 못 하는 것"인지 "우리가 매 프레임 호출해서 그런 것"인지 알 수 없다.

그래서 `ApplyPassthrough(bool force)` 하나로 두 모드를 갖고, `F3`으로 즉시 전환하게 했다.

- `on-change` — 폴리곤이 실제로 바뀐 경우에만 `DisplayServer`를 건드린다 (회피 가설)
- `every-frame` — 매 프레임 강제 호출 (버그 재현)

`_regionWrites` 카운터로 두 모드의 실제 호출 횟수 차이가 HUD에 보인다.
**every-frame에서만 깜빡이면 회피책이 통하는 것이고, 곧 Go 판정이다.**

### 계측을 앱 안에 넣은 이유

합격 기준이 "유휴 CPU < 1%, 메모리 < 150MB"인데 작업관리자 곁눈질로는 기록이 안 남는다.
`PerfProbe`가 작업관리자와 같은 공식으로 계산한다:

```
CPU% = (프로세스 CPU 시간 델타) / (실제 경과 시간 × 논리 코어 수) × 100
```

`F9`를 누르면 노션 측정 기록표에 그대로 붙일 형태로 클립보드에 복사된다.

### 현재 상태

| | 상태 |
|---|---|
| `dotnet build` | ✅ 오류 0 · 경고 0 |
| 에디터 실행 | ✅ 동작 확인 (육안) |
| **정량 측정** | ⬜ **미실시** — CPU/메모리 10분 방치 측정 아직 안 함 |
| 플리커 재현 실험 (F3) | ⬜ 미실시 |
| 멀티모니터 / DPI 검증 (F7/F8) | ⬜ 미실시 |
| 전체화면 게임 위 동작 | ⬜ 미실시 |
| 노트북 배터리 1시간 | ⬜ 미실시 |

**즉, "떴다"까지만 확인됐고 Day 1-2 합격 기준은 아직 아무것도 판정되지 않았다.**
측정 절차와 기록표는 [DAY1-2-SPIKE.md](DAY1-2-SPIKE.md) §3~§5에 있다.

### 다음

1. DAY1-2-SPIKE.md §3 측정 절차 수행 → §5 판정표 채우기
2. Day 2-3 글로벌 입력 스파이크 (프로젝트 최대 리스크)
   - `GetLastInputInfo` 폴백부터 30분 안에 만들어 안전망 확보
   - `RawInput` / `WH_KEYBOARD_LL` 비교 + VirusTotal · Defender · Vanguard 실측
3. 스팀 앱 수수료 결제 + 세금 서류 (개발과 병렬, 시계가 돌고 있음)

---

## 2026-09-13 — 개발 환경 / 워크플로 확정 (Visual Studio + Godot 에디터)

Godot 첫 프로젝트인데 팀의 배경이 언리얼이라, "어디까지가 에디터의 일이고 어디부터가
Visual Studio의 일인가"를 확정하지 않은 채로 작업하면 계속 헤맨다. 그래서 이번에 정리했다.

### 한 일

1. **VS에서 F5 디버깅이 되게 함** → `tools/VsLauncher` 런처 프로젝트 추가
2. **언어 방침 확정** — 우리 코드는 C# 단일. GDScript는 서드파티 애드온이 들고 올 때만 허용
3. `src/*.cs.uid` 커밋, `.gitignore`에 `.vs/`·`.idea/` 추가, 문서의 구 프로젝트명 경로 정리

### 언어: C# 단일로 간다

Godot은 노드 하나에 스크립트 하나를 붙이는 구조라 C#과 GDScript를 같은 트리에 섞을 수 있다.
하지만 경계를 넘는 호출이 `node.Call("method_name", args)` 형태의 **문자열 기반 동적 디스패치**다.
메서드명 오타, 인자 개수 실수, 이름 변경 리팩터링이 전부 런타임에야 터진다.
2인 / 1개월에서 낼 비용이 아니다.

| 대상 | 언어 |
|---|---|
| 우리가 쓰는 게임·셸 코드 | **C#** |
| 서드파티 애드온이 GDScript로 와 있는 경우 (`addons/`) | 그대로 쓰고 얇은 C# 래퍼로 격리 |
| 튜토리얼·문서·StackOverflow | 전부 GDScript. **읽고 번역하는 비용은 계속 든다** |

마지막 줄은 WEEK0 §9가 지적한 A안의 실질 리스크 그대로다. 다만 이것은 *섞어 쓰는* 비용이
아니라 *읽는* 비용이므로, GDScript를 프로젝트에 들이는 것으로 해결되지 않는다.
대응은 "GDScript를 읽을 수 있을 정도로만 익히기"뿐이다.

### 언리얼 ↔ Godot 대응

| 언리얼 | Godot |
|---|---|
| `.uproject` | `project.godot` (파일) |
| `Intermediate/`, `DerivedDataCache/` | `.godot/` (폴더, 순수 캐시) |
| `Content/` + `.uasset` | 프로젝트 폴더 전체. Content/Source 구분 없음 |
| `Source/` 모듈 DLL | `ProjectSeWoo.dll` 하나 |
| Generate VS Project Files | **없음.** csproj가 `**/*.cs` glob이라 파일 추가 시 재생성 불필요 |
| Live Coding | **없음.** 정지 → 빌드 → 재실행 |
| UnrealVersionSelector (`.uproject` 더블클릭) | **없음.** `project.godot`에 "연결 프로그램"을 직접 지정해야 함 |
| Epic Launcher Library | Project Manager (exe를 인자 없이 실행) |

### PIE에 대응하는 것은 없다. 둘로 쪼개져 있다

언리얼 PIE는 게임이 에디터 프로세스 *안에서* 돌기 때문에 디버거 하나로 전부 본다.
**Godot은 게임이 항상 별도 프로세스다.** 에디터에서 F5를 눌러도 게임은 자식 프로세스로 뜬다.
그래서 PIE가 한 번에 주던 두 가지가 서로 다른 도구로 갈린다.

| PIE가 주던 것 | Godot에서는 |
|---|---|
| C# 중단점 / 스텝 / 로컬 변수 | **Visual Studio** |
| 실행 중 씬 트리 보기, 노드 프로퍼티 실시간 수정 | **Godot 에디터 `디버거` 패널 → `Remote` 탭** |

| 실행 방법 | C# 중단점 | Remote 씬 트리 |
|---|---|---|
| VS에서 F5 (`VsLauncher`) | O — `_Ready()` 등 기동 코드도 잡힌다 | X (에디터가 띄운 게 아니라 연결되지 않음) |
| Godot 에디터에서 F5 | `Ctrl+Alt+P` 연결 시 O (기동 코드는 놓친다) | O |

"로직 버그 추적"이냐 "눈으로 확인·튜닝"이냐로 띄우는 쪽을 고른다. 둘 다 필요한 경우는 드물다.
(미검증 아이디어: 에디터 `디버그 > 디버그 서버 계속 열어두기` + 게임에
`--remote-debug tcp://127.0.0.1:6007` 인자를 주면 양쪽을 동시에 얻을 수 있을 가능성이 있다)

### 확정된 작업 루프

```
[코드]  VS 열고 수정 → Ctrl+Shift+B (에러를 VS 에러 목록에서 먼저 잡기) → F5
        시작 프로젝트는 VsLauncher 로 고정해 둔다. 왕복할 필요 없다.

[씬/에셋/프로젝트 설정]  project.godot 더블클릭 또는 Project Manager → Godot 에디터

[동시] 에디터로 씬 짜면서 VS로 F5 디버깅 가능. 프로세스가 달라 공존한다.
       드물게 어셈블리 파일 락이 나면 한쪽에서만 빌드한다.
```

**빌드가 언제 필요한가** — 에디터를 열기 전에 미리 빌드해 둘 필요는 없다.
에디터는 필요하면 스스로 빌드하고, 에디터에서 F5를 눌러도 stale하면 빌드한다.
명시적 빌드(에디터 우상단 망치 버튼)가 필요한 경우는 하나뿐이다:
**새 C# 클래스나 `[Export]` / `[Signal]`을 추가해서 그것이 에디터 UI(노드 추가 목록, 인스펙터)에
나타나게 하고 싶을 때.** 에디터는 소스를 파싱하지 않고 컴파일된 어셈블리에서 타입 정보를 읽는다.

빌드 출력은 `bin/`이 아니라 **`.godot\mono\temp\bin\Debug\ProjectSeWoo.dll`** 이다.
`Godot.NET.Sdk`가 경로를 리다이렉트한다. `.gitignore`의 `.godot/` 한 줄에 이미 걸린다.

### `tools/VsLauncher` — VS에서 F5가 안 되는 문제의 해결

`ProjectSeWoo`는 **클래스 라이브러리**다. 우리가 게임 exe를 빌드하는 것이 아니라
**Godot exe가 호스트이고 `ProjectSeWoo.dll`을 로드**하는 구조이기 때문이다.
VS는 시작 프로젝트가 될 수 있는지를 출력 형식으로 먼저 판단하고, Library면
실행 프로필을 아예 읽지 않는다. `.csproj.user`에 넣어도, 속성 UI로 넣어도 무시된다.
(Visual Studio 기준. VS Code + Godot Tools 나 Rider는 이 시나리오를 정식 지원한다)

그래서 **시작 가능한 Exe 프로젝트를 통행증으로 하나 두고 실행 대상만 바꿔치기한다.**

```
F5
 → ① VsLauncher 는 OutputType=Exe → "시작 가능" 판정 통과
 → ② 솔루션 빌드 (ProjectSeWoo.dll + pdb 갱신)
 → ③ launchSettings.json 의 commandName="Executable" 프로필이
      VsLauncher.exe 대신 Godot exe 를 디버거 아래에서 실행
      (VsLauncher 의 코드는 한 줄도 실행되지 않는다)
 → ④ Godot 이 게임으로 부팅 → ProjectSeWoo.dll 로드
 → ⑤ VS 가 어셈블리 로드를 감지해 옆의 pdb 로 중단점 바인딩
```

`tools/VsLauncher/Properties/launchSettings.json`의 프로필 이름이 VS 툴바 드롭다운에
그대로 나오므로, 이름에 판단 근거를 박아 두었다:

| 프로필 | 인자 | 용도 |
|---|---|---|
| `Godot 게임 (중단점 O)` | `--path .` | 평소 디버깅 |
| `Godot 에디터 (중단점 X)` | `--path . -e` | VS에서 에디터를 열 때 |

팀원 셋업은 `launchSettings.json`의 `executablePath` **한 줄**을 자기 엔진 경로로 고치는 것뿐이다.

### 밟은 함정 (이어서. 1~5는 2026-09-01 항목)

**6. VS는 출력 형식이 클래스 라이브러리인 프로젝트를 시작할 수 없다**
위 `tools/VsLauncher` 항목 참고. `.csproj.user`나 속성 UI로는 우회 불가.

**7. 에디터 플래그 `-e`를 주면 중단점이 절대 걸리지 않는다**
`-e`를 주면 에디터가 뜨고, 에디터의 F5는 게임을 *자식 프로세스*로 띄운다.
디버거는 에디터에 붙어 있으므로 게임 코드 중단점이 걸릴 수 없다.
`-e` 없이 `--path`만 주면 게임이 그 프로세스에서 직접 돌아 중단점이 걸린다.

**8. `project.godot`에 주석을 쓰지 마라**
Godot 에디터가 이 파일을 재저장할 때 **주석을 보존하지 않는다.** 실제로 2026-09-01에
넣어둔 주석이 전부 날아갔다. 또 기본값과 같은 항목은 생략하므로
`window/stretch/mode="disabled"`도 사라졌다(기본값이므로 동작은 동일).
→ 설계 근거 메모는 `docs/`나 C# 코드 주석에 둔다.

**9. `Godot.NET.Sdk`는 프로젝트 폴더 하위의 모든 `.cs`를 컴파일한다**
`tools/`를 그냥 만들면 런처 코드가 `ProjectSeWoo.dll` 안에 들어간다.
`<Compile Remove="tools/**" />`로 MSBuild 쪽을, `tools/.gdignore`로 Godot 에디터 쪽을 막았다.

**10. VS의 시작 프로젝트는 `.sln`이 아니라 `.suo`에 저장된다**
`.sln`을 손으로 고쳐 프로젝트를 추가해도 시작 프로젝트는 바뀌지 않는다.
`.vs\ProjectSeWoo\v18\.suo`에 들어 있고 VS UI에서만 바꾼다
(`VsLauncher` 우클릭 → `시작 프로젝트로 설정`, 설정되면 이름이 굵게 표시된다).
증상: 빌드는 성공한 뒤에 "시작할 수 없습니다" 문구가 뜬다.
또 솔루션이 열린 상태에서 `.sln`을 외부에서 고쳤다면 솔루션을 닫고 다시 열어야 반영된다.

**11. `.cs.uid` / `.import`는 커밋 대상이다**
Godot 4.4+는 스크립트에도 `uid://`를 부여하고 `파일명.cs.uid`로 저장한다. 파일을 옮기거나
이름을 바꿔도 씬 참조가 깨지지 않게 하는 안정 ID다. 커밋하지 않으면 팀원 쪽에서 새로 생성돼
값이 갈리고, 나중에 씬 참조 충돌이 난다. (`.godot/`는 반대로 캐시이므로 커밋하지 않는다)

### 환경 갱신 (2026-09-01 기록과 달라진 것)

| | 2026-09-01 | 현재 |
|---|---|---|
| .NET SDK | 9.0.301 | **10.0.400** (10.0.102도 설치됨) |
| .NET 8 런타임 | 8.0.17 | 8.0.23 |
| 엔진 위치 | `~/Desktop/...` | **`C:\Tools\Godot_v4.7.2-stable_mono_win64\...`** |

SDK가 10이어도 `net8.0` 타깃 빌드·실행 모두 정상이다(경고 0 · 오류 0 확인).
**`net10.0`으로 올리지 않는다.** Godot 4.7이 검증한 조합은 `net8.0`이고, 벗어나면
얻는 것 없이 리스크만 늘어난다.

### 현재 상태

| | 상태 |
|---|---|
| `dotnet build ProjectSeWoo.sln` | ✅ 경고 0 · 오류 0 (두 프로젝트) |
| VS F5 디버깅 | ✅ 동작 확인 |
| 게임 어셈블리에 런처 코드 미포함 | ✅ 타입 목록으로 확인 |
| **Day 1-2 정량 측정** | ⬜ **미실시 — 2026-09-01과 동일. 아직 아무 기준도 판정되지 않았다** |

### 다음

개발 환경이 정리됐으므로 더 이상 코드를 쌓기 전에 측정을 끝낸다.

1. `DAY1-2-SPIKE.md` §3 측정 절차 수행 → §4 기록표 · §5 판정표 채우기
   - 메모리 150MB를 못 맞추면 첫 번째 카드는 **렌더러를 Forward+ → Compatibility로 변경**.
     투명 창에 스프라이트 몇 개 띄우는 상주 앱에 Forward+/D3D12는 과하다.
     단, 변경 후 per-pixel 투명이 그대로 되는지 반드시 재확인.
2. Day 2-3 글로벌 입력 스파이크 — `GetLastInputInfo` 안전망부터 30분 안에
3. 스팀 앱 수수료 결제 + 세금 서류 (개발과 병렬)

---

## 2026-09-13 (2) — Day 1-2 측정 착수 · 플리커 Go, 메모리 미판정

측정 절차와 기록은 [DAY1-2-SPIKE.md](DAY1-2-SPIKE.md) §3~§5. 여기에는 판정과 그 근거만 남긴다.

### 결과

| 항목 | 결과 | 값 / 근거 |
|---|---|---|
| **플리커 (godot#80098)** | **Go** | `F3` 양쪽 모드 모두 깜빡임 없음. 4.7.2에서 수정된 것으로 판단 |
| **유휴 CPU** | **Go** | `avg 0.11%` / `peak 1.17%` (기준 1%의 1/9). 121초 측정 |
| 투명 / 무테 / 항상 위 / 클릭 통과 | Go | 창 정상 기동, `F2` 토글 동작 |
| **메모리** | **미판정** | 지표를 잘못 골라 측정했다. 아래 참고 |
| 멀티모니터 좌표 | **측정 불가** | 이 PC는 모니터 1대 (`screen #0 of 1`) |
| DPI 125% 좌표 / 드래그 / 전체화면 게임 위 / 배터리 | 미실시 | |

**플리커가 Go로 나온 것이 이번의 가장 큰 소득이다.** Go/No-Go 매트릭스에서 이것만이
"근본적으로 안 됨"이 나올 수 있는 항목이었다. CPU나 메모리는 설정으로 깎을 수 있지만
렌더링이 깜빡이는 것은 우리가 고칠 수 없다. 남은 항목은 전부 설정/설계로 대응 가능한
범주이거나 단순 미실시다.

### 메모리를 판정하지 못한 이유 — 계측 지표를 잘못 골랐다

첫 측정에서 `329MB`가 나왔는데 내역이 맞지 않았다: `godot 81MB + clr heap 2MB = 83MB`인데
전체가 329MB. 원인은 `PerfProbe`가 `Process.WorkingSet64`를 쓰고 있었던 것이다.
이 값은 **공유 페이지를 포함한 전체 작업 집합**이고, D3D12 런타임 / GPU 드라이버 /
.NET 런타임의 DLL 페이지가 모두 들어간다. 우리 앱의 점유를 과대평가한다.

세 지표를 구분해야 한다. 대체로 `개인 작업 집합` <= `개인 커밋` <= `전체 작업 집합`이다.

| 지표 | 성격 | 어디서 |
|---|---|---|
| 전체 작업 집합 | 공유 DLL 페이지 포함 → 과대평가 | HUD `ws` |
| 개인 커밋 | 이 프로세스만 책임지는 커밋. **판정 기준** | HUD `priv`, 작업관리자 `커밋 크기` |
| 개인 작업 집합 | 물리 RAM 중 우리만 쓰는 부분. **유저가 보는 값** | 작업관리자 `메모리`(기본 열) |

`PrivateCommitMb`를 추가하고 주석을 고쳤다. 정식 재측정은 다음 세션.

**참고값** (판정 근거 아님) — 세션 도중 `Win32_PerfFormattedData_PerfProc_Process`로 관측한
Godot 프로세스: `PrivWS 236MB` / `WS 355MB` / `PrivCommit 351MB`.
측정 시점의 HUD 설정을 확인하지 않았고 관측 프로세스가 게임인지 에디터인지도 확정하지 않았다.
다만 세 지표가 모두 150MB를 넘으므로 정식 측정에서도 초과할 가능성이 높다고 보고
렌더러 A/B 표를 미리 준비해 두었다 (DAY1-2-SPIKE.md §4).

### 밟은 함정 (이어서. 1~11은 앞 항목)

**12. 리포트에 하드코딩한 합격 기준 문자열이 판정으로 오독된다**
`BuildReport()` 마지막 줄이 `"PASS: idle cpu < 1%, memory < 150MB, no flicker, ..."`
라는 고정 문자열이었다. 기준을 적어둔 메모인데 `PASS`로 시작해서 통과로 읽힌다.
실제로 메모리가 떨어지고 플리커는 테스트도 안 한 결과를 통과로 오독할 여지가 있었다.
→ 숫자 항목은 `[auto]`로 실제 PASS/FAIL을 계산하고, 눈으로 봐야 하는 항목은 `[eye] ?`로
남겨 사람이 채우게 바꿨다. **계측 도구가 판정을 사칭하지 않게 하는 것이 중요하다.**

**13. 플리커 테스트는 passthrough를 켜고 해야 한다**
`F2`가 꺼진 상태에서 `F3`을 눌러도 매 프레임 *빈 폴리곤*을 쓰는 것이라
(`OverlayShell.BuildRegion`이 빈 배열을 반환) 재현 조건이 아니다.
첫 측정이 `passthrough off` 상태였다.

**14. 플리커 관찰 배경은 어두운 쪽과 밝은 쪽 둘 다 필요하다**
DAY1-2-SPIKE.md가 "흰 배경을 두고"라고 지시했는데 불완전했다.
**흰색 번쩍임은 흰 배경 위에서 보이지 않는다.** 아티팩트가 흰색인지 검은색인지 모르므로
한쪽만 보면 놓친다. 문서를 고쳤다.

**15. `RenderingServer.GetCurrentRenderingMethod` / `...DriverName` 은 `OS`가 아니다**
`OS`에 있을 것 같지만 `RenderingServer`에 있다. `OS`에는 `GetVideoAdapterDriverInfo`만 있다.

**16. Windows 11 작업관리자에서 `커밋 크기` 열 찾기가 번거롭다**
`세부 정보` 탭에서 열 머리글 우클릭 → `열 선택` → 목록에서 찾아야 한다.
`프로세스` 탭의 열 목록에는 없다. PowerShell 한 줄이 더 확실하다
(DAY1-2-SPIKE.md §3-1에 명령을 적어 두었다).
덧붙여 **프로세스 이름은 `ProjectSeWoo`가 아니다.** Godot exe가 호스트이고 우리 dll이
얹혀 있으므로 `Godot_v4.7.2-stable_mono_win64.exe`로 찾아야 한다.

### 다음

1. **메모리 정식 재측정** — standalone 실행(VS F5 금지), 에디터 닫고, `pass on`/`cap 60`/
   `lowpower on` 확인 후 `priv` 판정. 30초 값과 10분 값을 비교해 누수도 같이 본다
2. 초과하면 렌더러 A/B (DAY1-2-SPIKE.md §4). `gl_compatibility`가 가장 가벼울 후보이나
   per-pixel 투명이 유지되는지 반드시 확인
3. DPI 125% 좌표 확인 (`F8` 외곽선 대비 실제 클릭 영역) — 모니터 1대로도 가능
4. 멀티모니터는 **팀원 B 환경 또는 외부 모니터 연결 필요.** 이 PC에서는 불가능한 항목으로
   확정하고 별도 일정으로 빼낸다
5. Day 2-3 글로벌 입력 스파이크 — `GetLastInputInfo` 안전망부터

---

## 2026-09-15 — A3~A8 일괄. 갑 쪽 W1~W2 물량 소진, 남은 리스크는 을 착수

**로그가 09-13 이후로 비어 있었다.** 그 사이 A3~A8과 씬 구조 재정비가 전부 들어갔고,
각 일감의 상세는 이미 전용 문서로 나가 있다. 여기는 이 기록의 원칙대로 **판정과
근거, 그리고 전용 문서에 안 적히는 것**만 남긴다.

| 일감 | 결과 | 문서 |
|---|---|---|
| A3 셸 모듈화 · `IShell` 실물 · 세이브 파일 I/O | 완료 | [A3-SHELL-MODULE.md](A3-SHELL-MODULE.md) |
| A4 글로벌 입력 | 완료. **타건 즉시 연출은 불가 제약으로 확정** | [A4-GLOBAL-INPUT.md](A4-GLOBAL-INPUT.md) |
| A5 커서 꾸미기 · `ICursorLayer` 실물 | 완료. A2 스파이크 코드 정리 | [A5-CURSOR-COSMETICS.md](A5-CURSOR-COSMETICS.md) |
| A6 트레이 · 자동 시작 · 옵션 창 | 완료. 세이브 스키마 v2 | [A6-TRAY-OPTIONS.md](A6-TRAY-OPTIONS.md) |
| A7 저부하 실측 | **메모리 기준 재설정 → `PrivWS < 300MB`, 조건부 Go** | [A7-PERF.md](A7-PERF.md) |
| 씬 구조 재정비 | `IInteractiveArea` 계약, `game/` 골격 | [SCENE-ARCHITECTURE.md](SCENE-ARCHITECTURE.md) |
| 을 시작 가이드 | 배포 완료. **을은 갑 실물을 하나도 안 기다려도 된다** | [B-TRACK-START.md](B-TRACK-START.md) |
| **A8 Steam 연동** | **완료. 아래에 자세히** | [A8-STEAM.md](A8-STEAM.md) |

세이브 경로는 `%APPDATA%/PunchMonkey/`로 확정했다. 게임명 최종 확정(§12, 스팀 검색
중복 확인)은 여전히 별개로 남아 있고, 이름이 바뀌면 `project.godot`의
`custom_user_dir_name` 한 줄만 고치면 된다.

### A8 — 기획서 문구를 뒤집었다: GodotSteam → Steamworks.NET

기획서 §9는 "Steam SDK(**GodotSteam**) 연동"으로 적혀 있었다. **Steamworks.NET
(순수 C# 바인딩, SDK 1.60)으로 갔다.**

근거는 하나다 — **이 프로젝트에 GDScript가 한 줄도 없다.** GodotSteam은 GDExtension
이라 C#에서는 모든 호출이 `Engine.GetSingleton("Steam").Call("createLobby", ...)`
형태의 문자열 + Variant 마샬링이 되고, 시그니처 오타가 컴파일이 아니라 런타임에
터진다. A9~A11(로비 · P2P · 재접속)이 **전부 이 위에 얹히므로** 그 비용이 W3 내내
누적된다. GodotSteam의 장점(GDScript에서 바로 쓸 수 있음)은 우리에게 해당이 없다.

**이 변경이 을에게 안 새어나가는 것이 요점이다.** 을은 `IAchievements`만 보고 그
뒤가 무엇인지 모른다. A1이 인터페이스를 먼저 고정해 둔 설계가 여기서 실제로 값을
했다 — 기획서 문구를 뒤집었는데도 `game/`에 영향이 0이다. §8-2 규칙대로
[A1-CONTRACTS.md](A1-CONTRACTS.md) §0에 **변경 이력 표를 새로 만들어** 날짜와 사유를
남겼다(그동안 규칙만 있고 표가 없었다).

### A8 설계에서 실제로 고민한 것 — "스팀 없이도 앱은 온전하다"

보통 스팀 게임은 스팀이 없으면 안 켜진다. **우리는 그러면 안 된다.** 상주
오버레이라 스팀을 안 켠 채 켜져 있는 시간이 더 길고, A6 자동 시작을 켜면 로그인
직후 — 스팀이 뜨기 한참 전에 — 우리가 먼저 뜬다.

그래서 초기화 실패를 **오류가 아니라 상태**로 만들었다. 여기서 두 가지가 따라 나왔다.

1. **30초 재시도.** 한 번 실패하고 끝내면 그 세션 내내 멀티·도전과제가 죽는다.
   유저가 나중에 스팀을 켜도 소용없다. 부팅 자동 시작이면 첫 시도는 거의 항상 실패한다
2. **`RestartAppIfNecessary`를 쓰지 않는다.** 스팀 게임의 표준 관행이지만, 부팅
   자동 시작 앱이 이걸 부르면 **유저가 요청한 적 없는 스팀을 강제로 띄우고 자기를
   재실행한다.** 최악의 경우 부팅 때마다 반복된다. 빌드 제출(A12) 때 한 번 더 확인한다

### A8 실측 — 스팀은 개인 커밋 +24MB, CPU는 영향 없음

A7 메모리 게이트가 **조건부 Go**인 상태에서 새 네이티브 런타임을 얹는 작업이라,
안 재고 넘어가면 나중에 원인을 못 찾는다. 무인 측정에서 스팀을 일부러 켜는
`--steam` 스위치를 그 목적으로 넣었다(기본은 꺼짐 — 아래 함정 20).

| 조건 (커서 포함, Debug) | 개인 커밋 | 작업 집합 | 유휴 CPU |
|---|---|---|---|
| 스팀 없음 | 355 MB | 354 MB | 0.20% |
| **스팀 있음** | **379 MB** | 380 MB | 0.15% |
| 차이 | **+24 MB** | +26 MB | 오차 범위 |

**CPU는 영향이 없다.** 콜백 펌프를 매 프레임 돌리는데도 §7-3의 1% 기준 안이다.
**메모리는 여유가 줄었다** — 기준(`PrivWS < 300MB`) 안이긴 하나, A7이 추정 근거로
쓴 출발점 자체가 올라갔다. **A7 §5의 남은 항목인 Release `PrivWS` 직접 측정은
이제 `--steam`을 켠 조건으로 해야 한다.**

연결 자체는 `--steam-selftest`(헤드리스, 종료 코드 0/1)로 확인했다 — 실제 계정
연결·통계 수신 PASS. 도전과제 ID는 **읽기만** 한다. `Unlock`을 자체 검사에 넣으면
나중에 진짜 앱 ID로 이 검사를 돌렸을 때 실제 도전과제가 해금돼 버린다.

### 밟은 함정 (이어서. 1~16은 앞 항목)

**17. `using Godot;` 이 있으면 `Environment`가 모호 참조다**
Godot에 3D 환경 리소스 `Environment`가 있어서 `Environment.SetEnvironmentVariable`이
컴파일 오류가 난다. `System.Environment`로 명시해야 한다. **`platform/` 파일이 전부
`using Godot;`을 달고 있으므로 `System.*`의 흔한 이름을 쓸 때마다 재발한다.**

**18. `Callback<T>` 핸들을 필드로 안 붙들면 콜백이 조용히 죽는다**
`Callback<UserStatsReceived_t>.Create(...)`의 반환값을 버리면 GC가 걷어가고, 그때부터
`RunCallbacks()`가 아무것도 부르지 않는다. **예외도 로그도 없다 — 그냥 영원히 안 온다.**
A9~A11에서 로비/P2P 콜백을 붙일 때 같은 함정이 그대로 반복된다.

**19. NuGet에 네이티브가 없고, `lib/`에 있는 건 참조 어셈블리다**
`Steamworks.NET` NuGet에는 관리 어셈블리만 들어 있다 — `steam_api64.dll`은 GitHub
릴리스에서 **같은 태그**로 따로 받아야 한다(버전이 어긋나면 `Packsize.Test()`에서
갈린다). 게다가 `lib/netstandard2.1/`의 것은 실행용이 아닌 **참조 어셈블리**이고
실제 구현은 `runtimes/<rid>/`에만 있다. Godot의 로드 컨텍스트가 `.deps.json`의
`runtimeTargets`를 읽어서 정상 해석됐지만, **운이 좋은 게 아니라 확인해야 아는
것이었다.** 엔진 버전이 올라가면 다시 봐야 한다.

**20. 무인 측정에서 스팀이 켜지면 안 된다**
`--report=` 측정이 스팀을 켜면 친구 목록에 "게임 중"이 뜨고 그 자체가 측정에 잡히는
부하다. `measure-renderers.ps1`은 조건마다 재시작하므로 네 번 깜빡인다. A6 트레이
아이콘이 이미 같은 이유로 `_unattended`에 묶여 있었고, 스팀도 같은 플래그에 묶었다.
**프로세스 밖으로 새어나가는 부작용은 전부 이 플래그 하나로 모은다**는 규칙이
세 번째로 값을 했다(세이브 파일 → 레지스트리 → 스팀).

나머지 A8 함정(`steam_appid.txt`의 작업 디렉터리 의존 등)은 [A8-STEAM.md](A8-STEAM.md) §5.

### 다음

**개발이 아닌 것이 먼저다.**

1. **스팀 Direct 수수료 $100 결제 상태 확인 (§11).** 결제 후 30일 대기다. 기획서가
   "W1 Day 1에 확인하라"고 했는데 아직 체크박스가 비어 있고, 두 세션 연속 "다음"으로만
   밀렸다. **미결제면 10월 중순 출시가 산술적으로 불가능하다.** A8이 480(Spacewar)
   임시 ID로 돌고 있는 것도 이것 때문이다
2. **게임명 확정** (W1 마감 항목, 이미 지연). 세이브 경로가 `PunchMonkey`로 굳었으니
   스팀 검색 중복 확인만 하면 된다
3. **을 착수 확인.** [B-TRACK-START.md](B-TRACK-START.md)는 어제 다 썼다. 남은 건
   "읽었고 시작했다"는 응답을 받는 것이다. **A8까지 끝나 갑 쪽 W1~W2 물량이 사실상
   다 돌았으므로, 지금 가장 큰 리스크는 기술이 아니라 을 착수다.** 안 붙으면 갑이
   B1을 직접 시작할지 §10 컷 라인을 당겨 쓸지 결정해야 한다
4. **Release export 템플릿 설치 → `PrivWS` 직접 측정** (`--steam` 조건으로).
   조건부 Go를 확정 Go로 올리는 마지막 단계이고, A12 빌드 파이프라인에서 어차피
   템플릿이 필요하므로 묶으면 이득이다
5. A9~A11(로비 / P2P / 재접속)은 A8 위에 바로 얹을 수 있다. 다만 **진짜 앱 ID 없이
   어디까지 검증되는지**를 먼저 확인할 것 — 480으로 로비까지는 되지만 우리 도전과제는
   안 걸린다
6. DPI 125% 좌표 확인(`F8`)은 여전히 이월. 멀티모니터는 이 PC(1대)에서 불가능

---

## 2026-09-16 — 게임명 `PunchMonkey` 확정. 작업 트리 비움

W2 3일차. 개발 물량이 아니라 **09-15 "다음" 목록에서 두 세션 연속 밀려 있던 행정
항목 하나를 닫았다.**

### 게임명 — 코드 식별자는 따라가지 않는다

스팀 검색 중복 없음을 확인하고 **`PunchMonkey`로 확정**했다(기획서 §11 체크박스,
§12 항목 닫음). 세이브 경로가 이미 `%APPDATA%/PunchMonkey/`로 굳어 있었으므로
(09-15) 경로 변경은 없다.

**여기서 정한 것은 게임명이 어디까지 퍼지는가다.** 두 이름을 분리해서 쓴다.

| | 이름 | 이유 |
|---|---|---|
| 유저에게 보이는 것 | `PunchMonkey` | 창 제목, 작업관리자, 세이브 폴더, 익스포트 산출물, 스토어 |
| 코드 식별자 | `ProjectSeWoo` | `.sln` / `.csproj` / 네임스페이스 / `assembly_name` |

바꾼 것은 네 곳뿐이다 — `project.godot`의 `config/name`, `export_presets.cfg`의
`export_path`(`build/PunchMonkey.exe`), `tools/inspect-windows.ps1`의 `-Name` 기본값
(익스포트 exe 이름으로 프로세스를 찾으므로 같이 따라간다), 그리고 DAY1-2-SPIKE.md의
릴리스 익스포트 명령 한 줄.

**네임스페이스를 안 바꾼 이유가 요점이다.** `ProjectSeWoo.*`는 `game/`·`shared/`·
`platform/` 전 파일과 을에게 나간 [B-TRACK-START.md](B-TRACK-START.md) 예제 코드에
박혀 있다. 을이 착수도 하기 전에 전 파일 `using`을 갈아엎으면 얻는 것(내부 이름의
미관)보다 잃는 것(가이드 문서와 실제 코드의 불일치, 착수 지연)이 크다. `assembly_name`도
같이 둔다 — 바꾸면 `ProjectSeWoo.dll`을 이름으로 붙들고 있는 `VsLauncher`/A7 측정
스크립트/`.csproj` 주석이 전부 딸려 온다. **유저는 dll 이름을 보지 않는다.**

`tools/measure-renderers.ps1`은 손댈 필요가 없었다 — 프로세스명을 `-Exe` 경로에서
파생시키고 있어서 exe 이름이 바뀌어도 그대로 따라간다. 하드코딩을 안 해 둔 게
여기서 값을 했다.

> DAY1-2-SPIKE.md §3의 **09-14 실측 기록에 남은 `build\ProjectSeWoo.exe`는 안 고쳤다.**
> 그때 실제로 그 파일을 측정했기 때문이다. 기록은 기록대로 두고, 앞으로 실행할
> 명령만 고친다.

### 작업 트리 — 두 건 다 커밋

09-15 이후 트리에 두 파일이 떠 있었다. 을이 `game/`에 붙기 전에 비워 뒀다.

1. **`tools/VsLauncher/Program.cs`** — `GODOT` 환경변수가 없을 때 쓰는 폴백 경로에
   압축 해제 폴더명이 한 겹 더 들어가 있었다. 환경변수를 설정해 둔 이 PC에서는
   **이 줄을 탈 일이 없어서 안 드러났다.** 을이 환경변수 없이 VS F5를 누르는 순간
   터지는 자리였다 — B-TRACK-START.md §2가 `VsLauncher`를 시작 프로젝트로 지정하라고
   안내하고 있으므로 첫 실행에서 바로 만난다
2. **`platform/Shell.tscn`** — 에디터로 열었다 저장하면서 씬 uid와 스크립트
   `ext_resource`의 uid가 붙었다. 참조하는 `uid://c15368wl7s4k1`이 이미 커밋된
   `platform/OverlayShell.cs.uid`와 일치하므로 내용 변화는 없다. **을이 §4-2대로
   `GameRoot.tscn`을 이 씬에 끌어다 넣을 예정이라 미리 커밋했다** — 안 그러면
   그 커밋 diff에 무관한 uid 잡음이 섞인다

### 다음

09-15 목록에서 2번(게임명)만 빠졌다. **나머지는 그대로 남아 있고, 1번의 시한이
오늘 지난다.**

1. **스팀 Direct 수수료 $100 결제 상태 확인 (§11).** 결제 후 30일 대기이므로
   **오늘 결제해도 10/16이다.** 출시 목표가 10월 중순이니 오늘이 산술적 마지노선이고,
   하루만 더 밀리면 10월 중순은 성립하지 않는다. 세 세션 연속 "다음"으로 밀렸다.
   A8이 480(Spacewar) 임시 ID로 도는 것도 이것 때문이다
2. **을 착수 확인.** `game/`은 아직 `.gitkeep` 5개뿐이다. 안 붙으면 갑이 B1을 직접
   잡을지 §10 컷 라인을 당겨 쓸지 **결정해야 한다** — W3(9/21)에 A9~A11이 들어오면
   갑은 B를 대신 못 잡는다
3. Release export 템플릿 설치 → `PrivWS` 직접 측정 (`--steam` 조건). A7 §5 마지막
   항목이고 A12에서 어차피 템플릿이 필요하다. **산출물 경로가 `build/PunchMonkey.exe`로
   바뀌었으니 측정 명령도 이 이름으로 돈다**
4. A9 착수 전, 진짜 앱 ID 없이 480으로 어디까지 검증되는지 범위부터 긋기
5. DPI 125% 좌표 확인(`F8`)은 여전히 이월. 멀티모니터는 이 PC(1대)에서 불가능

---

## 2026-09-16 (이어서) — A7 Release 실측: `PrivWS` 239MB, **확정 Go**

A7 §5의 마지막 항목을 닫았다. 상세 수치는 [A7-PERF.md](A7-PERF.md) §3, 여기는 판정과
**전용 문서에 안 적히는 것**만 남긴다.

### 막혀 있던 게 이미 안 막혀 있었다

A7 §3은 "export 템플릿이 이 PC에 없다"로 끝나 있었고, 나는 몇백MB~1GB 다운로드를
각오하고 들어갔다. **이미 설치돼 있었다** (`%APPDATA%\Godot\export_templates\
4.7.2.stable.mono\`, 27개 파일, `windows_release_x86_64.exe` 포함). `version.txt`가
`4.7.2.stable.mono`로 엔진과 일치하는 것까지 확인하고 바로 익스포트로 넘어갔다.

**문서에 "못 했다"고 적힌 항목은 착수 전에 현재 상태부터 확인해야 한다.** 세션이
끊긴 사이에 해결돼 있을 수 있고, 여기서는 그게 30분짜리 다운로드였다.

### 판정 — 확정 Go. 그리고 스팀 비용의 해석이 바뀌었다

| 조건 | `priv`(커밋) | **OS `PrivWS`** | cpu avg |
|---|---|---|---|
| 스팀 off (2회) | 343 | **235** | 0.22% |
| **스팀 on (3회)** | 367 | **239** | 0.14% |

기준 `PrivWS` < 300MB 대비 **61MB(20%) 여유**. §4가 비율 0.71로 추정한 276MB보다
**37MB 가벼웠다** — 실제 비율은 0.65다. 추정이 안전한 쪽으로 틀렸다.

**여기서 A8의 결론 하나를 다시 읽어야 한다.** A8은 "스팀이 +24MB, 메모리 여유가
줄었다"고 썼다. 커밋 기준 +24MB는 **Release에서도 정확히 재현됐다**(343→367).
그런데 **정식 기준인 `PrivWS`로는 +4MB뿐이다**(235→239). 스팀 런타임은 커밋을 크게
예약하지만 실제로 상주시키는 페이지는 훨씬 적다. **A8이 틀린 게 아니라, 09-16에
기준 지표를 바꾼 뒤로 같은 사실의 의미가 달라진 것이다.** 지표를 바꿀 때는 그
지표로 내렸던 과거 판정을 다시 읽어야 한다는 게 교훈이다.

부수적으로 **`gl_compatibility` 재도전 카드는 철회했다.** 20% 여유가 실측으로
확인된 이상, 플리커 재검증(§3-2)이라는 비싼 값을 치를 이유가 없다.

### `--steam-selftest`가 무인 플래그를 빠져나가고 있었다

익스포트 바이너리에서 스팀이 붙는지 확인하려고 `--steam-selftest`를 **창 있는 채로**
돌렸다. A8은 이걸 항상 헤드리스로만 돌렸다. 그랬더니:

```
[autostart] 해제
[tray] indicator id=0
[steam] 연결됨 appid=480 user=... (Spacewar 임시 ID)
```

**트레이 아이콘이 떴고, 레지스트리 Run 키를 건드렸고, 세이브 파일을 덮었다.**
`IsUnattendedRun()`이 `--selftest`(정확히 일치)와 `--report=`(접두사)만 보는데
`--steam-selftest`는 **둘 다 아니다.** 헤드리스면 `DisplayServer.GetName()`으로
걸리니까 A8에서는 안 드러났을 뿐이다.

이 함수의 주석에는 **"프로세스 밖으로 새어나가는 부작용을 전부 막는다"**고 적혀
있고, A3에서 세이브 파일을 덮은 사고 때문에 만든 플래그다. **그 플래그를 인자
이름으로 하나씩 열거하는 방식이 다음 인자에서 바로 샜다.** 접미사 매칭으로 고쳤다 —
`--net-selftest` 같은 게 A9~A11에서 늘어도 같은 실수가 안 난다.

피해는 없었다. Run 키에 우리 항목이 애초에 없었고(`autostart: false`),
`[autostart] 해제`는 없는 것을 다시 없앤 no-op였다. **세이브 파일은 실제로
덮였지만**(창 위치만) 개발용 값이었다.

### 리포트가 스팀을 켰는지 스스로 말하지 못했다

첫 측정 3회가 `PrivWS` 290 → 240 → 236으로 흔들렸는데, **리포트에 스팀 줄이 없어서
어느 회차가 스팀을 켠 것인지 판별할 방법이 없었다.** 스팀 여부가 이 측정의 주제인데
측정 결과가 그걸 안 적고 있었다. `BuildReport()`에 한 줄 넣고 다시 쟀더니 5회가
234~241에 몰렸고(§3-2), 290은 첫 회차 단 한 번이었다.

**290의 원인은 특정하지 못했다.** 첫 실행 셰이더 캐시 워밍이 유력하지만 재익스포트
뒤 첫 실행에서는 재현되지 않았다 — 분리해서 확인한 게 아니므로 A14(첫 실행 플로우)로
넘긴다. 유저의 최초 실행이 한 번 무겁다는 뜻일 수 있다.

같은 김에 리포트의 `[auto] private commit < 150MB FAIL` 줄을 내렸다. **09-16에 기준을
`PrivWS` < 300MB로 바꿔 놓고 리포트는 옛 기준으로 FAIL을 찍고 있었다** — 표는 Go,
리포트는 FAIL. `PrivWS`는 프로세스가 자기 자신에 대해 싸게 못 구하므로(WMI로 밖에서
찍는다) 리포트는 참고값만 남기고 판정은 `measure-renderers.ps1` 표에서 한다.

### 익스포트에서 밟은 것

**21. `Start-Process -ArgumentList` 는 인자를 따옴표로 안 묶는다**
`@('--export-release','Windows Desktop',...)`을 넘기면 `Windows`와 `Desktop`이 따로
간다. `Invalid export preset name: Windows.` 로 즉사한다. 배열 원소 안에 따옴표를
직접 넣어야 한다(`'"Windows Desktop"'`).

**22. `build/`가 자기 자신의 pck 안으로 들어가고 있었다**
`export_filter="all_resources"`에 `exclude_filter`가 비어 있어서, **직전 익스포트
산출물이 다음 pck 안에 들어간다.** pck가 7KB → 39KB로 불었다. 09-14 첫 익스포트
때는 `build/`가 비어 있어서 안 보였고, 두 번째 익스포트에서야 드러나는 구조다.
`exclude_filter="build/*"`로 막았다. **`.godot/*`는 같이 제외하면 안 된다** —
임포트된 텍스처(`.godot/imported/`)와 `uid_cache.bin`이 거기 있어서 아이콘이 깨진다.

**23. 헤드리스 익스포트가 안 죽는 건 재현되지 않았다**
DAY1-2-SPIKE.md가 "패킹까지 `DONE` 찍고 CPU 0%로 남는다"고 적어 둔 함정인데,
`exclude_filter` 넣은 뒤로는 세 번 다 `exit 0`으로 자체 종료했다. **`build/`를
자기 안에 packing 하던 것과 관련이 있어 보이지만 확인한 것은 아니다.** CI에
타임아웃+kill은 그대로 두는 게 맞다.

**24. 익스포트에 `InputHelper.exe`가 없다**
셀프테스트가 `[input] 헬퍼 실행 파일 없음`을 찍었다. **익스포트한 빌드는 전역
타건을 못 받는다.** A4가 별도 프로세스로 간 대가다 — Godot export 는 우리
`platform/InputHelper/` 산출물을 모른다. `steam_api64.dll`과 같은 성격의 누락이고,
**A12가 반드시 처리해야 한다.** 이번 측정값에는 영향이 없다(별도 프로세스라
이 프로세스의 `PrivWS`에 안 잡힌다). 다만 **출시 빌드의 실제 상주 발자국은
`PunchMonkey.exe` + `InputHelper.exe` 둘의 합**이라는 것은 따로 기억해 둔다.

### 도구 쪽 변경

`measure-renderers.ps1`에 `-Only`(조건 하나만)와 `-GameArgs`(게임에 인자 전달)를
넣었다. 렌더러를 고르는 A/B는 끝났고, 지금 필요한 건 **채택된 렌더러로 Debug와
Release를 비교**하는 것이라 네 조건을 다 돌 이유가 없다. 표의 굵게 표시도
`priv < 150` 에서 `PrivWS < 300`으로 옮겼다 — 안 옮기면 전부 탈락으로 보인다.

### 다음

1. **스팀 Direct $100 결제 확인.** 오늘도 안 닫혔다. 결제 후 30일이라 **오늘이
   10월 중순 출시의 산술적 마지노선이다**
2. **을 착수 확인.** `game/`은 여전히 `.gitkeep` 5개
3. A9 착수 전, 480으로 어디까지 검증되는지 범위 긋기
4. A12 — `steam_api64.dll` 복사 + `InputHelper.exe` 동봉을 익스포트 스크립트로.
   이번에 손으로 한 순서(익스포트 → dll 복사)가 그대로 스크립트가 된다
5. DPI 125% 좌표 확인(`F8`) 이월

---

## 2026-09-16 (셋째) — 스팀 수수료 결제. 앱 ID `5281130`, **마지막 날에 닫혔다**

**세 세션 연속 밀려 있던 §11 1번 항목이 오늘 닫혔다.** 결제 후 30일 대기이므로
**10/16 이후 출시 가능** — 10월 중순 목표가 산술적으로 성립하는 마지막 날이었다.
하루만 더 밀렸으면 출시일 자체를 옮기는 논의부터 해야 했다.

### 교체는 상수 하나지만, 이름이 이미 하나 차 있었다

`SteamService.SpacewarAppId = 480` 자리를 `DefaultAppId = 5281130` 으로 바꿨다.
처음에 `AppId` 라는 이름을 썼다가 **같은 클래스에 `public uint AppId => _appId`
프로퍼티가 이미 있어서** 컴파일이 깨졌다. 상수는 "기본값", 프로퍼티는 "지금 실제로
쓰는 값"이라 둘 다 필요하다 — `DefaultAppId` 로 갈랐다.

**480 은 지우지 않고 진단용으로 남겼다.** `--steam-appid=480` 으로 되돌려 보면
"우리 앱 설정 문제인가, SDK/로컬 환경 문제인가"를 한 번에 가를 수 있다. 다만
**로비(A9)를 480 으로 시험하면 안 된다** — 전 세계 SDK 예제 사용자와 같은 로비
공간을 쓰게 된다. 주석에 박아 뒀다.

### 진짜 ID 로 바꾸니 도전과제가 오히려 안 온다 — 예정된 상태다

```
[steam] 연결됨 appid=5281130 user=ggoggal627 id=76561198411431220
[steam] 통계 수신 실패 (k_EResultFail) - 도전과제 비활성
initialized : PASS
stats/ach   : FAIL
```

**480 일 때는 `stats/ach : PASS` 였다.** 겉보기로는 진짜 앱 ID 로 바꿔서 기능이
깨진 것처럼 보이지만 반대다 — 480 에서 통과했던 건 **Spacewar 의 도전과제 5개**가
잡힌 것이지 우리 것이 아니었다. 우리 앱에는 파트너 사이트에 통계·도전과제 스키마가
아직 없어서 `UserStatsReceived` 가 `k_EResultFail` 로 온다. **A15 에서 등록하면
풀린다.**

**이걸 지금 기록해 두는 이유는, 나중에 이 로그를 보고 "언제부터 깨졌지"를
뒤지게 되기 때문이다.** 임시 ID 가 진짜 ID 보다 더 많은 걸 통과시키는 구간이
있었다는 게 직관에 반한다.

계약은 그대로 작동한다. `IsAvailable` 이 false 라 `Unlock` 은 조용히 버려지고
`IsUnlocked` 는 false 다 — A1 §2-1 이 정한 그대로고, **을 쪽 코드는 영향이 0 이다.**
해금 조건 로직은 `MockAchievements` 로 끝까지 시험할 수 있다.

부수적으로 셀프테스트 `elapsed` 가 0.3s → 10.0s 로 늘었다. 실패 콜백을 기다리느라
타임아웃까지 간다. **종료 코드는 여전히 0 이다** — 판정이 `IsInitialized` 기준이기
때문이다. `IsAvailable` 로 올리면 A15 전까지 항상 1 이 되므로, 올리는 건 A15 뒤다.

### A7 판정은 앱 ID 와 무관했다

A7 의 Release 실측 5회는 전부 480 으로 잰 값이었다. 진짜 ID 로 한 번 더 쟀다 —
`priv` 369 / `PrivWS` **239** / cpu 0.21%. **차이 없다.** 스팀 런타임 비용은 앱 ID 와
무관하고, 확정 Go 판정은 그대로 유효하다. 재는 데 1분이면 되는 것을 안 재고 넘어가면
나중에 "그 측정은 임시 ID 였잖아"가 남는다.

### 도전과제는 아직 등록하지 않았다 — 의도적이다

파트너 사이트에서 지금 바로 등록할 수 있지만 **안 했다.** API Name 4개 중 3개가
`ACH_KEYSTROKES_10K` / `100K` / `1M` 로 **수치를 이름에 박고 있는데, 그 수치가
아직 잠정이다.** 기획서 §6 은 "로그 스케일 레벨 환산" 만 확정했고 구간은 B3(레벨
곡선)와 W2 밸런스 튜닝 뒤에 다시 보기로 한 값이다. **스팀에 등록한 API Name 은
나중에 못 바꾼다.** 지금 등록하면 밸런싱 결과가 "5만이 맞다"로 나와도
`ACH_KEYSTROKES_10K` 라는 이름을 출시 후까지 안고 간다. A15 가 W4 에 있는 이유다.

### 다음

**§11 의 남은 행정 항목이 이제 경로 위에 있다.**

1. **W-8BEN 세금 서류 · 은행 계좌 확인** — 승인이 안 나면 출시가 아니라 정산이
   막힌다. 결제 흐름에 이어서 처리
2. **스토어 페이지("출시 예정") 공개** — 출시 최소 2주 전, 늦어도 W3 중.
   10/16 출시 가능일 기준이면 **10/2 가 마지노선**이다. W4 는 C1(캡슐·스크린샷·
   트레일러)과 겹치므로 소재 준비가 여기서 앞당겨진다
3. **을 착수 확인.** `game/` 은 여전히 `.gitkeep` 5개. **오늘부로 프로젝트에서 가장
   큰 리스크는 단독으로 이것이다** — 기술도 행정도 다 닫혔다
4. A9 착수. 이제 진짜 앱 ID 로 로비를 시험할 수 있다 (480 금지, 위 참고)
5. A12 — `steam_api64.dll` 복사 + `InputHelper.exe` 동봉 스크립트화
6. A15 전에 마일스톤 수치 확정 (B3 + W2 밸런싱 결과 반영)

---

## 2026-09-16 (넷째) — A12 빌드 파이프라인. `build/dist` 183MB, 검증 4종 통과

상세는 [A12-BUILD.md](A12-BUILD.md). 여기는 **판단이 필요했던 것**만.

### 헬퍼를 어떻게 뽑을지가 A12 의 실제 내용이었다

`steam_api64.dll` 복사는 A8 이 예고해 둔 대로였다. **문제는 `InputHelper.exe` 였고,
그냥 복사하면 되는 게 아니었다.**

`InputHelper.csproj` 는 `SelfContained=false` 다. 기본 publish 는 150KB apphost +
dll 이고 **유저 PC 에 .NET 8 런타임이 있어야 돈다.** 게임 본체는 Godot 이 런타임을
`data_.../` 에 76.5MB 통째로 동봉해서 상관없는데, **헬퍼는 그걸 못 쓴다** —
`hostfxr` 이 `Program Files\dotnet\shared` 를 보지 옆 폴더의 사설 복사본을 안 본다.

클린 PC 에서 어떻게 되냐면, **헬퍼만 조용히 안 뜬다.** 창은 뜨고 원숭이도 보이고
크래시도 없고 **타건만 영원히 0** 이다. 스크린샷으로는 정상인 빌드가 나간다.

| 방식 | 크기 | 런타임 의존 |
|---|---|---|
| 프레임워크 의존 (기본) | 150KB | **있음 — 탈락** |
| 자체 포함 단일 파일 | ~70MB | 없음 |
| **NativeAOT** | **1.8MB** | 없음 |

**AOT 로 갔다.** 70MB 를 아끼는 것보다, 자체 포함 단일 파일이 실행할 때마다 자기를
임시 폴더에 푸는 동작이 **A13(백신 검증)과 상성이 나쁘다**는 쪽이 더 컸다. 상주 앱의
보조 프로세스는 기동이 잦다.

대가는 **빌드 머신에 MSVC C++ 빌드 도구 의존**이다. 스크립트가 없으면 명확히 죽게
했고, 주석에 "프레임워크 의존으로 우회하지 말 것" 을 박아 뒀다 — 우회하면 위 실패가
그대로 돌아오는데, 그때는 원인이 안 보인다.

### 밟은 것

**25. `vswhere` 가 PATH 에 없으면 MSVC 가 있어도 AOT 가 실패한다**
에러가 `link.exe` 를 가리켜서 **C++ 도구가 없는 것처럼 보인다.** 실제로는 AOT 링크
단계가 `vswhere.exe` 로 MSVC 를 찾다가 실패하고, 그 실패 메시지가 링커 명령줄 안으로
섞여 들어간다. 설치 경로(`C:\Program Files (x86)\Microsoft Visual Studio\Installer`)를
PATH 앞에 붙이면 바로 된다. 스크립트가 처리한다.

**26. `Start-Process -PassThru` 의 종료 코드는 `-Wait` 없이는 빈 값이다**
Windows PowerShell 5.1 에서 `-Wait` 를 준 경우에만 `.ExitCode` 가 채워진다.
`WaitForExit()` 을 직접 불러도 비어 있다(세 조건으로 확인했다). **그런데 빈 값은
비교에서 조용히 거짓이 된다** — 검증이 실제로는 PASS 인데 화면에는
`스팀 초기화 FAIL (종료 코드 )` 로 찍혔다. 숫자가 빠진 걸 눈으로 보고서야 알았다.
`-Wait` 에는 타임아웃이 없어서 빌드가 영원히 멈출 수 있으므로, .NET `Process` 를
직접 쓰는 `Invoke-Native` 로 갈았다.

**검증 코드가 조용히 무의미해지는 것이 검증이 없는 것보다 나쁘다.** 없으면 없는 줄
알지만, 있으면 통과했다고 믿는다.

**27. 새 ps1 은 UTF-8 BOM 으로 저장해야 한다**
BOM 없이 저장했더니 `powershell -File` 이 ANSI 로 읽어서 한글 주석이 깨지고,
깨진 바이트가 따옴표를 망가뜨려 **파싱 에러가 스크립트 중간 여기저기서** 났다.
원인이 인코딩인데 에러는 문법으로 보인다. 기존 `tools/*.ps1` 이 전부 BOM 이 있는
이유가 이것이었다.

### 검증을 4종으로 나눈 이유

동봉됐는가(파일 존재)와 **도는가**는 다르다. NativeAOT 는 마샬링이 걸린 코드를
조용히 망가뜨릴 수 있고, 헬퍼가 죽어도 게임은 멀쩡히 뜬다. 그래서 12초짜리
`--report=` 실행을 넣어 **심장박동까지 받는다** (`헬퍼 정상`). 12초를 쓸 값은 한다.

도전과제 통계는 **판정에 안 넣었다.** A15 전까지 `k_EResultFail` 이 정상이라
(A8 §4-3) 넣으면 빌드가 W4 까지 계속 빨간불이다.

### 결과

```
[6] 검증
      스팀 초기화 PASS (종료 코드 0)
      앱 ID 5281130 일치
      헬퍼 경로 해석됨
      헬퍼 실행 확인 (심장박동 수신)

완료 — build\dist  (파일 191개, 183 MB, 커밋 e644fcb)
```

SteamPipe VDF 생성도 붙였다(`-DepotId`). **`setlive` 는 일부러 비워 둔다** —
채우면 업로드가 곧바로 그 브랜치에 공개된다. 검수 전 빌드가 유저에게 나가는 사고를
스크립트가 만들면 안 된다.

### 다음

1. **Depot ID 확인** (파트너 사이트 > SteamPipe > Depots). 이것만 있으면 바로 올린다.
   앱 ID+1 로 추측하지 않는다 — 틀린 depot 에 올리면 되돌리기가 번거롭다
2. **A13 Defender / VirusTotal — 이 빌드로.** **서명 없는 1.8MB 네이티브 exe 가
   키보드 입력을 읽는다.** 휴리스틱 후보 1순위이고, AOT 채택으로 새로 생긴 변수다.
   코드 서명 여부는 이 결과를 보고 정한다
3. **을 착수.** `game/` 은 아직 `.gitkeep` 5개다. **지금 `build/dist` 에 들어 있는
   것은 자리표시자 마스코트뿐이다** — 이 빌드는 "파이프라인이 도는가" 의 증거이지
   게임이 아니다
4. 스토어 페이지 공개 마지노선 10/2 (출시 2주 전, 10/16 기준)

---

## 2026-09-16 (다섯째) — Depot `5281131` 확정. 상시 업로드 플로우 고정

Depot ID 를 받아 [A12-BUILD.md](A12-BUILD.md) §5~§6 을 채웠다. VDF 두 개가
`build/steampipe/` 에 나와 있고, 남은 건 빌드 계정과 steamcmd 뿐이다.

**depot 번호를 스크립트 기본값으로 박았다.** 앱 ID+1 인 것은 결과이지 규칙이
아니라서 확인 전에는 일부러 인자로 받게 해 뒀는데, 확인된 지금은 기본값이 맞다 —
"depot 번호 뭐였지" 를 매번 찾게 두면 언젠가 틀린 번호를 넣는다. `-DepotId 0` 이
VDF 생략이다.

### 업로드와 공개를 분리해 둔 것이 여기서 값을 한다

`setlive` 를 비워 두는 설계에 세 가지가 딸려 온다.

1. **올렸는데 공개는 아직** 이라는 상태가 존재한다. 이게 없으면 검수 전 빌드가
   한 번의 실수로 유저에게 나간다
2. **롤백이 업로드가 아니라 재지정이다.** 올린 빌드는 목록에 남으므로, 문제가
   생기면 이전 빌드를 브랜치에 다시 지정하면 끝이다 — 다시 빌드할 필요가 없다
3. **beta 에서 확인한 바이너리를 그대로 default 로 옮긴다.** "beta 에서 보고
   default 용으로 다시 빌드" 하면 확인한 것과 나가는 것이 다른 바이너리가 된다

### 자주 올리는 쪽이 싸다

SteamPipe 는 변경된 청크만 올린다. 우리 183MB 중 실제로 바뀌는 건 `.pck`(수십 KB)
와 `data_.../ProjectSeWoo.dll` 정도고, **104MB 짜리 `PunchMonkey.exe` 는 엔진
템플릿이라 엔진 버전을 올리기 전까지 안 바뀐다.** "무거우니 가끔만 올리자" 는
잘못된 절약이다 — 오래 안 올리면 한 번에 바뀐 양만 커진다.

### 상주 앱이라 업데이트가 늦게 붙는다

스팀은 게임이 꺼져 있을 때 업데이트한다. **우리는 며칠씩 켜져 있고**, 자동
시작(A6)까지 켠 유저면 더 길다. 올렸다고 유저가 받은 게 아니다 — A14(첫 실행
플로우)에서 같이 볼 항목으로 남겼다.

### 다음

1. **빌드 계정 생성 + steamcmd → 첫 업로드.** 지금 `build/dist` 에 있는 것은
   자리표시자 마스코트뿐이지만, **파이프라인 전 구간을 한 번 통과시켜 두는 것**이
   목적이다. 콘텐츠가 생긴 뒤에 처음 올리다 막히면 그때는 시간이 없다
2. **A13 Defender / VirusTotal — 이 빌드로.** 서명 없는 1.8MB 네이티브 exe 가
   키보드 입력을 읽는다. AOT 채택으로 새로 생긴 변수다
3. **을 착수.** `game/` 은 아직 `.gitkeep` 5개
4. 스토어 페이지 공개 마지노선 10/2

---

## 2026-09-16 (여섯째) — steamcmd 설치. 업로드 계정은 메인으로 간다

`C:\Tools\steamcmd\` 에 받아서 자체 업데이트까지 확인했다(Godot 과 같은 자리).
**남은 것은 대화형 로그인 한 번뿐이다** — Steam Guard 코드가 필요해서 사람이
직접 해야 하는 유일한 단계다.

### 계정 분리는 "규칙" 이 아니라 "CI 착수 조건" 으로 내렸다

[A12-BUILD.md](A12-BUILD.md) §5-4 에 빌드 계정을 따로 파는 절차를 적어 뒀는데,
**쓸 수 있는 스팀 계정이 `ggoggal627` 하나뿐이라 그걸로 간다.** 사용자 결정이다.

다만 "나중에 하자" 로 흘려보내지 않으려고 **분리 시점을 명시해서 박았다.**
개발 PC 한 대에서 사람이 직접 돌리는 동안은 자격 증명이 이 PC 밖으로 안 나가므로
위험이 크지 않다. **선을 넘는 것은 앱을 소유한 관리자 계정의 자격 증명이 빌드
서버에 올라가는 순간이고, 그때는 이미 여기저기 박혀 있어서 분리 비용이 훨씬
커진다.** 그래서 §6-1 의 "커밋 먼저" 같은 상시 규칙이 아니라 **CI 착수 조건**으로
성격을 바꿔서 남겼다 — 조건은 트리거가 오면 저절로 검토되지만, 규칙은 지키다
말면 그냥 사라진다.

### steamcmd 는 스팀 클라이언트와 자격 증명을 공유하지 않는다

클라이언트에 이미 로그인돼 있어도 steamcmd 는 처음 한 번 따로 로그인해야 한다.
**메인 계정으로 가더라도 §5-4 의 4번은 똑같이 필요하다** — "내 계정이니까 그냥
되겠지" 로 넘어갈 자리라 문서에 따로 적었다.

첫 실행에서 종료 코드 7 이 나오는데 오류가 아니다. 자체 업데이트 후 재실행하는
정상 경로이고, 두 번째 실행부터 0 이다.

### 다음

1. **대화형 로그인 → 첫 업로드.** 콘텐츠는 아직 자리표시자뿐이지만 목적은
   **파이프라인 전 구간을 한 번 통과시키는 것**이다
2. A13 Defender / VirusTotal — 이 빌드로
3. **을 착수.** `game/` 은 아직 `.gitkeep` 5개
4. 스토어 페이지 공개 마지노선 10/2

---

## 2026-09-16 (일곱째) — 첫 Depot 업로드. BuildID `25338283`

**파이프라인 전 구간이 한 번 통과했다.** 커밋 `8ad616a` → 빌드 → 검증 4종 →
steamcmd → depot 5281131. 업로드 자체는 **16초**였다(13:11:51 → 13:12:07).

```
Logging in using cached credentials.
[13:11:51]: Starting AppID 5281130 build (flags 0x0).
[13:11:51]: Building depot 5281131...
[13:12:07]: Successfully finished AppID 5281130 build (BuildID 25338283).
```

**아직 아무에게도 안 보인다.** `setlive` 를 비워 뒀으므로 브랜치에 안 붙었다 —
이게 설계대로 동작한 첫 사례다.

### "멈췄다" 가 세 번 나왔는데 원인이 세 번 다 달랐다

오늘 제일 오래 붙들린 곳이 빌드도 업로드도 아니고 **최초 로그인**이었다.

| 증상 | 실제 원인 |
|---|---|
| 응답 없음 | 에이전트 셸에 TTY 가 없어서 `password:` 가 오지 않는 stdin 을 기다림 |
| `ERROR (Invalid Password)` | persona name(`ggoggal627`)을 로그인 ID 로 사용. 진짜는 `swj627` |
| 120초 초과 | `+quit` 누락. **로그인은 이미 성공**했고 `Steam>` 프롬프트에서 대기 중이었다 |

**증상이 같다고 원인이 같지 않다.** 두 번째를 고치고 세 번째를 만났을 때, 나는
또 TTY 문제인가부터 의심했고 사용자는 예전에 받아둔 SteamworksSDK 충돌을
의심했다 — **둘 다 틀렸다.** `config/config.vdf` 에 계정 이름이 들어갔는지 본
것이 판별점이었다. 로그를 읽는 대신 **상태를 확인**해야 갈렸다.

두 번째 함정은 특히 악질이다. `ggoggal627` 도 실재하는 이름이라 스팀이 "그런
계정 없다" 가 아니라 **"비밀번호가 틀렸다"** 로 답한다. 원인이 아이디인데 에러는
비밀번호를 가리킨다. 우리 셀프테스트가 찍는 `user=` 는 `GetPersonaName()` 이라
앞으로도 계속 표시 이름이다 — 로그인 ID 는 A12-BUILD.md §5 에만 있다.

### 비밀번호가 대화 기록에 남았다

세션에서 바로 돌릴 수 있게 `+login <계정> <비밀번호> <가드코드>` 형태를 제시했고,
그게 실행되면서 **비밀번호가 평문으로 기록에 들어갔다.** 가드 코드는 일회용이라
무관하지만 비밀번호는 아니다. **비밀번호 교체 필요** — 캐시가 이미 생겼으므로
바꿔도 앞으로의 업로드는 그대로 돈다.

편의와 비밀 노출이 맞바꿔지는 자리에서 두 방법을 나란히 놓으면 편한 쪽이 선택된다.
**실제 창 로그인 하나만 안내했어야 했다.**

### 다음

1. **파트너 사이트에서 `25338283` 을 `beta` 브랜치에 지정** → 실제로 받아서 설치되는지
   확인. 여기까지 가야 파이프라인이 끝까지 검증된 것이다
2. **스팀 비밀번호 교체**
3. **A13 Defender / VirusTotal** — 이 빌드로. 서명 없는 1.8MB 네이티브 exe 가
   키보드 입력을 읽는다
4. **을 착수.** `game/` 은 아직 `.gitkeep` 5개. **오늘 갑 쪽은 A12 까지 끝났고
   행정도 다 닫혔다 — 남은 리스크는 이것 하나다**
5. 스토어 페이지 공개 마지노선 10/2

---

## 2026-09-16 (여덟째) — 배포 권한을 누가 가질지 결정. 준비 스크립트

### 결정: W3 까지 갑 단독, **9/28(W4 진입)에 을 추가**

을도 스팀에 올릴 수 있게 하자는 얘기가 나왔다. **지금은 아니다.**

**업로드가 병목이 아니다.** 16초짜리 작업이고 빈도는 주 1회(금요일 데모)다.
을이 못 해서 막히는 시나리오는 "금요일에 갑이 부재" 하나뿐이고 zip 으로 때워진다.
반면 **지금 을을 붙이는 비용은 작지 않다** — 을 PC 에 Godot export 템플릿
(수백MB~1GB)과 **MSVC C++ 빌드 도구**(InputHelper 를 NativeAOT 로 뽑느라)가
필요하다. `game/` 코드를 한 줄도 안 쓴 사람에게 지금 시킬 일이 아니다.

**9/28 이 적기인 이유**는 일정표가 이미 말하고 있다. W4 는 공동 작업 3.5d 가
각자에게 얹혀서 기획서 스스로 "실질 초과" 라고 적어 뒀다 — **직렬화가 실제로
아픈 유일한 구간이 거기다.** 그전까지 을의 병목은 빌드 권한이 아니라 B1~B9 다.

**계정 문제는 이미 풀려 있었다.** "쓸 수 있는 계정이 하나뿐" 이라 메인으로 갔는데,
**을은 자기 스팀 계정이 있다.** 그걸 파트너 그룹에 넣고 빌드 업로드 권한만 주면
자격 증명 공유가 전혀 없다 — A12 §5-4 의 "빌드 계정 따로 파기" 가 을 계정으로
자연히 해결된다. 지금 상태(메인 계정)보다 깨끗하다.

### 젠킨스를 안 쓰는 이유는 "과하다" 가 아니다

세 겹으로 안 맞는다.

1. **도구 선택이 틀렸다.** 이미 GitHub 원격이 있다. 젠킨스는 서버를 띄우고
   관리해야 하는데 2인 팀이 그럴 이유가 없다. 쓴다면 GitHub Actions 다
2. **우리 검증이 CI 에서 반쪽이 된다.** 이게 핵심이다 — 검증 4종 중 1번(스팀
   초기화)은 **스팀 클라이언트가 로그인된 채 떠 있어야** 돌고, 4번(헬퍼
   심장박동)은 **GUI 창을 12초 띄워야** 돈다. 헤드리스 러너에서는 둘 다 못 한다.
   **"빌드는 됐는데 실제로 도는지는 모르는" 상태가 되는데, 그건 A12 문서가
   통째로 경고하는 조용한 실패 그 자체다**
3. 나머지도 사람 손이 남는다. 브랜치 지정은 어차피 수동이고(그게 `setlive` 를
   비워둔 이유다), Steam Guard 를 CI 에 넘기려면 빌드 계정 + 공유 시크릿이 필요하다

**하게 된다면 답은 정해져 있다 — GitHub Actions + self-hosted runner(이 PC).**
스팀 클라이언트·GUI·export 템플릿·MSVC 가 한 번에 해결된다. 다만 **출시 이후다.**
지금 이틀 쓰면 그 이틀은 B 트랙에서 나온다.

### `tools/setup-steamcmd.ps1` + `.cmd`

9/28 에 을이 더블클릭 한 번으로 끝나게 미리 써 뒀다. **오늘 손으로 밟은 로그인
함정 셋을 스크립트가 대신 피한다.**

- **TTY 확인을 맨 앞에 뒀다.** 비대화형 셸이면 **다운로드 전에** 멈춘다. 설치를
  다 해놓고 마지막에 알게 되면 원인을 엉뚱한 데서 찾는다 (오늘 그랬다)
- 로그인 ID 를 물을 때 **"표시 이름이 아니다"** 를 같이 찍는다
- `+quit` 을 항상 붙인다
- **성공 판정을 종료 코드가 아니라 `config/config.vdf` 상태로 한다.** 오늘
  세 번째 막힘을 가른 것이 정확히 이 확인이었다

`.cmd` 래퍼를 같이 둔 이유는 둘이다. `.ps1` 은 **더블클릭해도 실행되지 않고**
편집기로 열리거나 실행 정책에 막힌다. 그리고 래퍼의 `pause` 가 **창을 열어 둔다** —
없으면 실패했을 때 에러를 읽기 전에 창이 닫힌다.

덤으로 Godot 템플릿·MSVC 존재를 **확인만** 한다(설치는 안 한다). 업로드에는 필요
없지만, **없다는 걸 금요일 데모 직전에 알면 그때는 기가 단위를 받을 시간이 없다.**

시험: 비대화형에서 가드가 걸려 exit 1, `-SkipLogin` 경로는 exit 0.

### 다음

1. **파트너 사이트 실행 옵션** — "잘못된 게임 구성" 미해결. depot 에 파일은
   있는데 **스팀이 뭘 실행할지 모른다.** 되면 A12 §5 에 "업로드만으로는 실행되지
   않는다" 로 박는다
2. 스팀 비밀번호 교체 (평문 노출)
3. A13 Defender / VirusTotal
4. **을 착수** — `game/` 은 아직 `.gitkeep` 5개
5. 9/28: 을 계정에 업로드 권한 + `setup-steamcmd.cmd`

---

## 2026-09-16 (아홉째) — 실행 옵션으로 "잘못된 게임 구성" 해결. 스팀 설정 문서화

**스팀에서 실제로 실행됐다.** 파이프라인이 커밋에서 실행까지 끝에서 끝까지 통과한
첫 사례다.

### 내 A12 문서에 구멍이 있었다

원인은 **파트너 사이트에 실행 옵션이 없던 것**이었다. depot 에 파일이 다 올라가도
**스팀은 그중 뭘 실행할지 모른다.**

[A12-BUILD.md](A12-BUILD.md) 를 쓰면서 **"depot 에 무엇을 올리는가" 까지만 다루고
"스팀이 그걸 어떻게 실행하는가" 를 통째로 빠뜨렸다.** 빌드 스크립트로 자동화되는
범위가 아니어서 시야에서 빠진 것이고, **저장소에 없고 파트너 사이트에만 있는
설정이 한 뭉치 더 있다**는 것을 놓쳤다. 새 depot 이나 새 플랫폼을 추가할 때 똑같이
밟을 자리라 [STEAM-CONFIG.md](STEAM-CONFIG.md) 로 분리하고 A12 §5 에 경고를 걸었다.

**작업 디렉터리를 비워도 됐던 것은 앞선 결정들의 배당금이다.** 우리 코드가 경로를
전부 exe 위치 기준으로 잡는다 — `ResolveHelperPath()` 가 `OS.GetExecutablePath()` 를
쓰고, A8 이 `steam_appid.txt`(작업 디렉터리 의존) 대신 환경변수로 갔다.
**그때는 "작업 디렉터리를 모른다" 가 문제였는데, 지금은 "설정할 게 하나 줄었다" 로
돌아왔다.**

### 설정값에서 기본값을 그대로 두면 안 되는 것 셋

- **Steam Cloud 는 끈다.** 직관과 반대인데, 클라우드 동기화는 **게임을 종료할 때**
  도는 것을 전제한다. **우리는 종료를 안 하는 앱이다.** A3 가 원자적 쓰기까지
  넣어둔 세이브에 Cloud 가 끼면 충돌 해소가 복잡해지고, 얻는 건 "다른 PC 에서
  이어하기" 인데 상주 오버레이는 보통 한 PC 에서 쓴다. **나중에 켜는 건 쉽고
  끄는 건 어렵다**
- **VAC 은 켜지 않는다.** 전역 키보드 RawInput 을 읽는 별도 프로세스가 있는 앱이
  VAC 대상이 되는 건 유저에게도 안티치트에게도 최악의 신호다. A13 이 검증으로
  다루는 축인데, **이쪽은 검증이 아니라 그냥 안 켜면 되는 일이다**
- **Steam Overlay 는 켜되 공짜가 아니다.** A9~A11 친구 초대가 오버레이 기반이라
  필요한데, 우리 창은 per-pixel 투명 + always-on-top + 클릭 통과에 d3d12 다.
  **오버레이 후킹이 스왑체인에 붙으면 투명이 깨질 수 있다** — A7 §3-2 플리커
  Go 판정은 **오버레이가 안 붙은 상태에서 받은 것**이다. A9 검증 항목으로 넘긴다

### 개인정보 문구는 검증 가능한 주장이다

스토어에서 제일 중요한 항목인데, 기획서 §7-6 에 문구가 이미 확정돼 있어서 새로
쓸 게 없었다. **다만 이게 마케팅 문구가 아니라는 점을 문서에 적어 뒀다.**

`RawKeyboardCounter.cs` 는 `RAWKEYBOARD` 를 통째로 마샬링하는 구조체를 만들지 않고
오프셋으로 `Flags` 만 읽는다 — **키 코드가 필드로 존재조차 하지 않는다.** 그 파일
주석이 이유까지 적어 뒀다("구조체를 만들면 안 읽는다가 규율의 문제가 된다").
**A4 때의 그 선택이 스토어 페이지에서 값을 한다** — "안 읽습니다" 를 코드로 증명할
수 있으니 강하게 써도 된다.

### 다음

1. **스토어 "출시 예정" 페이지 — 마지노선 10/2.** C1 소재가 그만큼 앞당겨진다
2. 스팀 비밀번호 교체 (평문 노출)
3. A13 Defender / VirusTotal
4. **을 착수** — `game/` 은 아직 `.gitkeep` 5개
5. A9 착수 시 **오버레이 켠 상태 투명 검증**
